export type ProxyItem = {
  id: string
  name: string
  proxy: string
  proxyType: string
  maxProfiles: number
}

export type ProxyFormValues = {
  name: string
  proxy: string
  proxyType: string
  maxProfiles: number
}
