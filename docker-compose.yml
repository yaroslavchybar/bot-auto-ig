services:
  anti:
    build: .
    image: yaroslavchybar/anti-instagram:latest
    container_name: anti-instagram
    stdin_open: true
    tty: true
    restart: unless-stopped
    volumes:
      - ./.env:/app/.env
      - ./data/profiles:/app/data/profiles
      - ./data/logs:/app/data/logs
